# Artefactos de build del host: la imagen compila su propio build/install/log
# dentro del contenedor, no necesitamos (ni queremos) los del host.
build/
install/
log/

# Lo mismo para el mini-workspace catkin anidado en src/code/code
# (frcobot_description ya tiene su propio COLCON_IGNORE, esto solo achica el
# contexto de build).
**/build/
**/install/
**/log/

**/__pycache__/
*.pyc

.git
.claude
